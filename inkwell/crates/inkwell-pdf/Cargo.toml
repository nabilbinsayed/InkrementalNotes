[package]
name = "inkwell-pdf"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PDFium integration for Inkwell: PDF normalisation, tile rasterisation, and text extraction."

[dependencies]
inkwell-core = { path = "../inkwell-core" }
pdfium-render = "0.9"
log = "0.4"

[dev-dependencies]
tempfile = "3"
